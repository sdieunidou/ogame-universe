{% extends 'base.html.twig' %}

{% block title 'Alliances' %}

{% block body %}
    <table class="table table-striped mt-5 table-dark table-data">
        <thead class="thead-dark">
            <tr>
                <th>Alliance</th>
                <th>Nom</th>
                <th>Site</th>
                <th>Joueurs</th>
            </tr>
        </thead>
        <tbody>
            {% for alliance in alliances %}
                <tr>
                    <td>
                        <a href="{{ path('app_alliance_view', {id: alliance.id}) }}">
                            {{ alliance.tag }}
                        </a>
                    </td>
                    <td>
                        {{ alliance.name }}
                    </td>
                    <td>{{ alliance.website }}</td>
                    <td>{{ alliance.players.count }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
